# Hello from *Seigneur* Chat App
 ## A hand crafted community chat client
 ### Installation
   * Clone the repository
   * run npm install or yarn install depending on your preferences

To start the application run the following command:
`electron .`
